#include <stdio.h>
int main()
{
  int i,j,k,n;
	printf("Enter range: ");
	scanf("%d",&n);
	for (i=1;i<=n;i++)
	{
		for(j=n-i;j>0;j--)
			printf("   ");
		for (k=1;k<=2*i-1;k++)
		{
			if(k%2==1)
				printf("%3d",i);
			else
				printf("   ");
		}
		printf("\n");
	}
 return 0;
}
